apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: flood-forecasting
    component: db
spec:
  selector:
    app: flood-forecasting
    component: db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
